# ERC-8183 ABIs

| Archivo | Uso |
|---------|-----|
| `AgenticCommerceV3.abi.json` | BSC BNB APEX (eventos estándar Virtual-style) |
| `AgenticCommerceHooked.abi.json` | Base UFX — `JobCreated` con `description` y solo `jobId`+`client` indexed |

Firmas validadas contra logs on-chain (Base Blockscout, BscScan).
